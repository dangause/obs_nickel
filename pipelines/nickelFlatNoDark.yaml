description: Flat ISR with optional dark
imports:
  - $CP_PIPE_DIR/pipelines/_ingredients/cpFlat.yaml

tasks:
  cpFlatIsr:
    class: lsst.cp.pipe.cpFlatIsr.CpFlatIsrTask
    config:
      doDark: false
